// Этот модуль - ранер для веб сервера.
// Он запускает http сервер на express и ws сервер на socket.io
const log = require('logger').common;
// const userModel = require('models/user');

const init = async () => {
  // log.info('');
  // await userModel.create({ name: 'Alex Litv'});
};

module.exports = init;
